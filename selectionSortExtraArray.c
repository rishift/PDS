#include <stdio.h>

int main() {

    int n;
    printf("Enter n: ");
    scanf("%d", &n);

    int A[1000], B[1000];

    printf("Enter elements: \n");

    for (int i = 0; i < n; i++)
        scanf("%d", &A[i]);

    for (int i = 0; i < n; i++) {
        int min = A[0];
        for (int j = 1; j < n; j++) {
            if (A[j] < )
        }
    }


    return 0;
}